<!doctype html>
<html>
  <head>
    <title><%=shopName%></title>
    <style>
      body {
        font-family: 'Arial', sans-serif;
        margin: 0;
        padding: 20px;
        background-color: #2c2c2c;
        color: #d4a574;
      }
      .nav {
        background: linear-gradient(135deg, #3a3a3a 0%, #2c2c2c 100%);
        padding: 18px;
        margin-bottom: 25px;
        border-radius: 10px;
        border: 2px solid #d4a574;
        box-shadow: 0 6px 20px rgba(212, 165, 116, 0.2);
      }
      .nav a {
        color: #d4a574;
        text-decoration: none;
        padding: 14px 24px;
        margin-right: 12px;
        font-weight: bold;
        border-radius: 6px;
        transition: all 0.3s ease;
        border: 1px solid transparent;
      }
      .nav a:hover {
        background-color: #d4a574;
        color: #2c2c2c;
        transform: translateY(-3px);
        box-shadow: 0 4px 15px rgba(212, 165, 116, 0.4);
        border: 1px solid #d4a574;
      }
      .container {
        max-width: 600px;
        margin: 0 auto;
        background: linear-gradient(135deg, #3a3a3a 0%, #2c2c2c 100%);
        padding: 40px;
        border-radius: 15px;
        border: 3px solid #d4a574;
        box-shadow: 0 8px 30px rgba(212, 165, 116, 0.25);
      }
      h1 {
        color: #d4a574;
        border-bottom: 3px solid #d4a574;
        padding-bottom: 18px;
        text-align: center;
        font-size: 2.2em;
        text-shadow: 2px 2px 8px rgba(0,0,0,0.5);
      }
      form {
        margin-top: 30px;
      }
      input[type="text"] {
        width: 100%;
        padding: 16px;
        margin: 12px 0 25px 0;
        border: 2px solid #5a5a5a;
        border-radius: 8px;
        font-size: 16px;
        background-color: #3a3a3a;
        color: #f0f0f0;
        transition: all 0.3s ease;
      }
      input[type="text"]:focus {
        outline: none;
        border-color: #d4a574;
        box-shadow: 0 0 15px rgba(212, 165, 116, 0.5);
        background-color: #4a4a4a;
      }
      input[type="submit"] {
        background: linear-gradient(135deg, #d4a574 0%, #e6b17e 100%);
        color: #2c2c2c;
        padding: 18px 50px;
        border: none;
        border-radius: 8px;
        cursor: pointer;
        font-size: 18px;
        font-weight: bold;
        transition: all 0.3s ease;
        display: block;
        margin: 30px auto 0;
        box-shadow: 0 6px 20px rgba(212, 165, 116, 0.4);
        text-transform: uppercase;
        letter-spacing: 1px;
      }
      input[type="submit"]:hover {
        background: linear-gradient(135deg, #e6b17e 0%, #d4a574 100%);
        transform: translateY(-4px);
        box-shadow: 0 8px 25px rgba(212, 165, 116, 0.6);
      }
      label {
        font-weight: bold;
        color: #d4a574;
        font-size: 17px;
        display: block;
        margin-bottom: 8px;
        text-shadow: 1px 1px 3px rgba(0,0,0,0.5);
      }
    </style>
  </head>
  <body>
    <div class="nav">
      <a href="/">Home</a>
      <a href="/about">About</a>
      <a href="/search">Search</a>
      <a href="/register">Register</a>
      <a href="/survey">Survey</a>
    </div>
    <div class="container">
      <h1>Discover Premium Drinks at <%=shopName%></h1>
      <form method="GET" action="/search_result">
        <label>Drink Name:</label>
        <input type="text" name="name_text" placeholder="Enter drink name" />
        
        <label>Category:</label>
        <input type="text" name="catagory_text" placeholder="Enter category" />
        
        <input type="submit" value="Search Collection" />
      </form>
    </div>
  </body>
</html>