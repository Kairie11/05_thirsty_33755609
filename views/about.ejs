<!doctype html>
<html>
  <head>
    <title><%=shopName%></title>
    <style>
      body {
        font-family: 'Arial', sans-serif;
        margin: 0;
        padding: 20px;
        background-color: #2c2c2c;
        color: #d4a574;
      }
      .nav {
        background: linear-gradient(135deg, #3a3a3a 0%, #2c2c2c 100%);
        padding: 18px;
        margin-bottom: 25px;
        border-radius: 10px;
        border: 2px solid #d4a574;
        box-shadow: 0 6px 20px rgba(212, 165, 116, 0.2);
      }
      .nav a {
        color: #d4a574;
        text-decoration: none;
        padding: 14px 24px;
        margin-right: 12px;
        font-weight: bold;
        border-radius: 6px;
        transition: all 0.3s ease;
        border: 1px solid transparent;
      }
      .nav a:hover {
        background-color: #d4a574;
        color: #2c2c2c;
        transform: translateY(-3px);
        box-shadow: 0 4px 15px rgba(212, 165, 116, 0.4);
        border: 1px solid #d4a574;
      }
      .container {
        max-width: 800px;
        margin: 0 auto;
        background: linear-gradient(135deg, #3a3a3a 0%, #2c2c2c 100%);
        padding: 40px;
        border-radius: 15px;
        border: 3px solid #d4a574;
        box-shadow: 0 8px 30px rgba(212, 165, 116, 0.25);
      }
      h1 {
        color: #d4a574;
        border-bottom: 3px solid #d4a574;
        padding-bottom: 18px;
        text-align: center;
        font-size: 2.4em;
        text-shadow: 2px 2px 8px rgba(0,0,0,0.5);
      }
      h2 {
        color: #e6b17e;
        margin-top: 40px;
        margin-bottom: 25px;
        text-align: center;
        font-size: 1.8em;
        text-shadow: 1px 1px 5px rgba(0,0,0,0.5);
      }
      p {
        line-height: 1.9;
        font-size: 18px;
        color: #f0f0f0;
        text-align: center;
        font-weight: 500;
        background: rgba(58, 58, 58, 0.8);
        padding: 25px;
        border-radius: 10px;
        border: 1px solid #d4a574;
      }
      .highlight {
        color: #e6b17e;
        font-weight: bold;
        text-shadow: 1px 1px 3px rgba(0,0,0,0.5);
      }
      .location-card {
        background: linear-gradient(135deg, #4a4a4a 0%, #3a3a3a 100%);
        margin: 20px 0;
        padding: 25px;
        border-left: 6px solid #d4a574;
        border-radius: 10px;
        border: 1px solid #5a5a5a;
        transition: all 0.3s ease;
      }
      .location-card:hover {
        transform: translateX(10px);
        box-shadow: 0 6px 20px rgba(212, 165, 116, 0.3);
        border-left: 6px solid #e6b17e;
      }
      .location-name {
        color: #d4a574;
        font-size: 1.4em;
        font-weight: bold;
        margin-bottom: 12px;
      }
      .manager-info {
        color: #e6b17e;
        font-size: 1.1em;
        margin-bottom: 8px;
      }
      .address-info {
        color: #f0f0f0;
        font-size: 1em;
        line-height: 1.6;
      }
    </style>
  </head>
  <body>
    <div class="nav">
      <a href="/">Home</a>
      <a href="/about">About</a>
      <a href="/search">Search</a>
      <a href="/register">Register</a>
      <a href="/survey">Survey</a>
    </div>
    <div class="container">
      <h1>About <span class="highlight"><%=shopName%></span></h1>
      <p>We specialize in <span class="highlight">premium beverages</span> including <span class="highlight">craft beers</span>, <span class="highlight">fine wines</span>, <span class="highlight">artisanal soft drinks</span>, and <span class="highlight">luxury hot drinks</span>. Experience the rich flavors and quality in every sip with our curated collection.</p>
      
      <h2>Our Locations</h2>
      <% for(let i = 0; i < shopLocations.length; i++) { %>
        <div class="location-card">
          <div class="location-name"><%= shopLocations[i].name %></div>
          <div class="manager-info">Manager: <%= shopLocations[i].manager %></div>
          <div class="address-info"><%= shopLocations[i].address %></div>
        </div>
      <% } %>
    </div>
  </body>
</html>