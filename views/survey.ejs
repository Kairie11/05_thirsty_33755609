<!doctype html>
<html>
  <head>
    <title>Customer Survey - <%=shopName%></title>
    <style>
      body {
        font-family: 'Arial', sans-serif;
        margin: 0;
        padding: 20px;
        background-color: #2c2c2c;
        color: #d4a574;
      }
      .nav {
        background: linear-gradient(135deg, #3a3a3a 0%, #2c2c2c 100%);
        padding: 18px;
        margin-bottom: 25px;
        border-radius: 10px;
        border: 2px solid #d4a574;
        box-shadow: 0 6px 20px rgba(212, 165, 116, 0.2);
      }
      .nav a {
        color: #d4a574;
        text-decoration: none;
        padding: 14px 24px;
        margin-right: 12px;
        font-weight: bold;
        border-radius: 6px;
        transition: all 0.3s ease;
        border: 1px solid transparent;
      }
      .nav a:hover {
        background-color: #d4a574;
        color: #2c2c2c;
        transform: translateY(-3px);
        box-shadow: 0 4px 15px rgba(212, 165, 116, 0.4);
        border: 1px solid #d4a574;
      }
      .container {
        max-width: 700px;
        margin: 0 auto;
        background: linear-gradient(135deg, #3a3a3a 0%, #2c2c2c 100%);
        padding: 40px;
        border-radius: 15px;
        border: 3px solid #d4a574;
        box-shadow: 0 8px 30px rgba(212, 165, 116, 0.25);
      }
      h1 {
        color: #d4a574;
        border-bottom: 3px solid #d4a574;
        padding-bottom: 18px;
        text-align: center;
        font-size: 2.2em;
        text-shadow: 2px 2px 8px rgba(0,0,0,0.5);
      }
      form {
        margin-top: 30px;
      }
      .form-group {
        margin-bottom: 30px;
      }
      label {
        font-weight: bold;
        color: #d4a574;
        font-size: 17px;
        display: block;
        margin-bottom: 12px;
        text-shadow: 1px 1px 3px rgba(0,0,0,0.5);
      }
      input[type="text"],
      input[type="email"],
      input[type="number"] {
        width: 100%;
        padding: 16px;
        border: 2px solid #5a5a5a;
        border-radius: 8px;
        font-size: 16px;
        background-color: #3a3a3a;
        color: #f0f0f0;
        transition: all 0.3s ease;
        box-sizing: border-box;
      }
      input[type="text"]:focus,
      input[type="email"]:focus,
      input[type="number"]:focus {
        outline: none;
        border-color: #d4a574;
        box-shadow: 0 0 15px rgba(212, 165, 116, 0.5);
        background-color: #4a4a4a;
      }
      .radio-group {
        background: rgba(58, 58, 58, 0.5);
        padding: 20px;
        border-radius: 8px;
        border: 1px solid #5a5a5a;
      }
      .radio-option {
        margin: 12px 0;
        display: flex;
        align-items: center;
      }
      input[type="radio"] {
        width: 20px;
        height: 20px;
        margin-right: 12px;
        cursor: pointer;
        accent-color: #d4a574;
      }
      .radio-option label {
        margin: 0;
        cursor: pointer;
        font-size: 16px;
        color: #f0f0f0;
        text-shadow: none;
      }
      .checkbox-group {
        background: rgba(58, 58, 58, 0.5);
        padding: 20px;
        border-radius: 8px;
        border: 1px solid #5a5a5a;
        display: flex;
        align-items: center;
      }
      input[type="checkbox"] {
        width: 22px;
        height: 22px;
        margin-right: 12px;
        cursor: pointer;
        accent-color: #d4a574;
      }
      .checkbox-group label {
        margin: 0;
        cursor: pointer;
        font-size: 16px;
        color: #f0f0f0;
        text-shadow: none;
      }
      input[type="submit"] {
        background: linear-gradient(135deg, #d4a574 0%, #e6b17e 100%);
        color: #2c2c2c;
        padding: 18px 50px;
        border: none;
        border-radius: 8px;
        cursor: pointer;
        font-size: 18px;
        font-weight: bold;
        transition: all 0.3s ease;
        display: block;
        margin: 40px auto 0;
        box-shadow: 0 6px 20px rgba(212, 165, 116, 0.4);
        text-transform: uppercase;
        letter-spacing: 1px;
      }
      input[type="submit"]:hover {
        background: linear-gradient(135deg, #e6b17e 0%, #d4a574 100%);
        transform: translateY(-4px);
        box-shadow: 0 8px 25px rgba(212, 165, 116, 0.6);
      }
    </style>
  </head>
  <body>
    <div class="nav">
      <a href="/">Home</a>
      <a href="/about">About</a>
      <a href="/search">Search</a>
      <a href="/register">Register</a>
      <a href="/survey">Survey</a>
    </div>
    <div class="container">
      <h1>Customer Survey</h1>
      <form method="POST" action="/survey_submit">
        <div class="form-group">
          <label>First Name:</label>
          <input type="text" name="first_name" placeholder="Enter your first name" required />
        </div>

        <div class="form-group">
          <label>Surname:</label>
          <input type="text" name="surname" placeholder="Enter your surname" required />
        </div>

        <div class="form-group">
          <label>Email Address:</label>
          <input type="email" name="email" placeholder="Enter your email" required />
        </div>

        <div class="form-group">
          <label>How old are you?</label>
          <input type="number" name="age" placeholder="Enter your age" min="1" max="120" required />
        </div>

        <div class="form-group">
          <label>Which of the following drinks categories do you consume?</label>
          <div class="radio-group">
            <% for(let i = 0; i < productCategories.length; i++) { %>
              <div class="radio-option">
                <input type="radio" id="category_<%= i %>" name="drink_category" value="<%= productCategories[i] %>" <%= i === 0 ? 'checked' : '' %> />
                <label for="category_<%= i %>"><%= productCategories[i] %></label>
              </div>
            <% } %>
          </div>
        </div>

        <div class="form-group">
          <label>Are you a student?</label>
          <div class="checkbox-group">
            <input type="checkbox" id="student" name="is_student" />
            <label for="student">Yes, I am a student</label>
          </div>
        </div>

        <input type="submit" value="Submit Survey" />
      </form>
    </div>
  </body>
</html>